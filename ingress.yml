apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: wanderift-ingress
annotations:
    kubernetes.io/ingress.allow-http: "true"
spec:
  rules:
  - host: wanderift.com
    http:
      paths:
      - path: /*
        backend:
          serviceName: wanderift
          servicePort: 80

